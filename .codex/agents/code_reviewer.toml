model = "gpt-5.3-codex"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = " Review changes using `git diff` to inspect all modified areas. Identify potential bugs, security issues, and any incomplete or missing implementation. Call out risky edge cases and suggest concrete fixes or follow-up checks. "
